import * as React from 'react';

import Header from '../src/components/Header';

export default {
  title: 'Header',
  component: Header,
};

export const PageHeader = (): React.ReactElement => <Header />;
